{
  "version": 2,
  "installCommand": "npm install && curl -sS https://getcomposer.org/installer | php -- --install-dir=/usr/local/bin --filename=composer && composer install --no-dev --optimize-autoloader",
  
  "buildCommand": "npm run build && php artisan optimize:clear && php artisan config:cache && php artisan optimize",
  
  "functions": {
    "api/index.php": {
      "runtime": "vercel-php@0.7.3"
    }
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/index.php"
    }
  ],
  "outputDirectory": "public"
}