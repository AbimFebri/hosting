{
  "version": 2,
  "installCommand": "npm install && composer install --no-dev --optimize-autoloader",
 
  "buildCommand": "npm run build && php artisan event:cache && php artisan view:cache && php artisan config:cache && php artisan route:cache && php artisan optimize",
  
  "functions": {
    "api/index.php": {
      "runtime": "vercel-php@0.7.3"
    }
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/index.php"
    }
  ],
  "outputDirectory": "public"
}